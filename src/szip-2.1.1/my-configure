. $OPA_HOME/src/ENV/$OPA_HOSTNAME
./configure --prefix=$OPA_HOME/HOST/$OPA_HOSTNAME
make clean
make
make install
ln -s $OPA_HOME/HOST/$OPA_HOSTNAME/lib/libsz.so.2.0.0 $OPA_HOME/HOST/$OPA_HOSTNAME/lib/libszip.so
